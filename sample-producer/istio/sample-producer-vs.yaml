apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: sample-producer-route
spec:
  hosts:
    - "*"
  http:
    - timeout: 30s
      route:
        - destination:
            host: sample-producer
            port:
              number: 8080
---
server:
  port: 8080

logging:
  level:
    root: info

spring:
  main:
    lazy-initialization: true
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"

remote:
  base:
    url: "http://sample-producer:8080"
